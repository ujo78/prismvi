[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = ". .venv/bin/activate && gunicorn --bind 0.0.0.0:$PORT dng_backend:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
