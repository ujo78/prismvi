[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = ". .venv/bin/activate && python dng_backend.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
