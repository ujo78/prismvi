[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m venv .venv",
  ". .venv/bin/activate && pip install --upgrade pip",
  ". .venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = ". .venv/bin/activate && python dng_backend.py"
